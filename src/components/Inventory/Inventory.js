import React from 'react';
import PropTypes from 'prop-types';

const NavBar = () => (
   <div>
      <h1>Inventory Page</h1>
   </div>
);

NavBar.propTypes = {};

NavBar.defaultProps = {};

export default NavBar;
